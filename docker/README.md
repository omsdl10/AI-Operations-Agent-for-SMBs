# Docker Notes

Docker Compose is defined at the repository root in `docker-compose.yml`.

The `docker/` directory is reserved for future deployment assets such as reverse proxy configuration, production compose overrides, and container entrypoint scripts.

